apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nats-server
spec:
  secretName: nats-server
  issuerRef:
    kind: ClusterIssuer
    name: le-prod
  dnsNames:
    - nats.k8s.nevi.network
  privateKey:
    algorithm: ECDSA
    size: 384
