apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: gateway-certs-share
  namespace: gateway
spec:
  provider:
    kubernetes:
      remoteNamespace: nats
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          name: gateway-certs-share
